# BUG_REPRO: 任务列表缓存内部别名 + 刷新覆盖历史快照

## Bug 是什么
`MissionStore.List` 返回内部缓存切片不拷贝、刷新时 `[:0]` 复用旧底层数组覆盖历史快照、列表不排序；`SurveyService` 缓存不改状态时不清除，列表仍显示旧状态。

## 如何触发
- 先取任务列表、修改状态后再取列表；
- 外部持有旧列表，再触发一次刷新。

## 错误信息
```text
返回的列表与内部缓存共享底层数组，外部修改污染了存储
历史快照被后续刷新改写
列表顺序不稳定: mission-bulk-o 在 mission-bulk-n 前面
服务层列表未反映最新状态: flying
```
