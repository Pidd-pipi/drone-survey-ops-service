# BUG_REPRO: typed-nil 校验器旁路 + 状态表未初始化

## Bug 是什么
`defaultStatusChecker` 常规分支返回 typed-nil 接口，调用 `Check` 即 panic；`missionStatuses` 懒初始化只建空 map 不填充，合法状态一直判未知。

## 如何触发
- 提交任意任务状态触发校验；
- 查询状态是否已知。

## 错误信息
```text
panic: value method drone-survey-ops-service.statusValidator.Check called using nil *statusValidator pointer [recovered]
goroutine 35 [running]:
drone-survey-ops-service.(*statusValidator).Check(0x1400010cf18?, ...)
        <autogenerated>:1 +0x88
drone-survey-ops-service.ValidateMissionStatus(...)
        .../backend/validation.go:14 +0x64
```
