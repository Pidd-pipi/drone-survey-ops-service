const items=document.querySelector('#items'),message=document.querySelector('#message');
async function load(){const r=await fetch('/api/missions');const missions=await r.json();items.innerHTML=missions.map(m=>`<tr><td>${m.id}</td><td>${m.site}</td><td>${m.pilot}</td><td>${m.images}</td><td>${m.battery_pct}%</td><td>${m.status}</td><td><button data-id="${m.id}">已降落</button></td></tr>`).join('');items.querySelectorAll('button').forEach(b=>b.onclick=async()=>{const r=await fetch('/api/missions/status',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:b.dataset.id,status:'landed'})});message.textContent=r.ok?'任务已完成':'更新失败';load()})}load();
